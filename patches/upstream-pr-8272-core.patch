diff --git a/dist/index.cjs.js b/dist/index.cjs.js
index 34d371f..b4d1b33 100644
--- a/dist/index.cjs.js
+++ b/dist/index.cjs.js
@@ -443,7 +443,7 @@ const buildUrlParams = (params, shouldEncode = true) => {
                 item += `${key}=${encodedValue}&`;
             });
             // last character will always be "&" so slice it off
-            item.slice(0, -1);
+            item = item.slice(0, -1);
         }
         else {
             encodedValue = shouldEncode ? encodeURIComponent(value) : value;
@@ -452,7 +452,7 @@ const buildUrlParams = (params, shouldEncode = true) => {
         return `${accumulator}&${item}`;
     }, '');
     // Remove initial "&" from the reduce
-    return output.substr(1);
+    return output.substring(1);
 };
 /**
  * Build the RequestInit object based on the options passed into the initial request
diff --git a/dist/index.js b/dist/index.js
index fb980f2..9f95d1d 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -439,7 +439,7 @@ const buildUrlParams = (params, shouldEncode = true) => {
                 item += `${key}=${encodedValue}&`;
             });
             // last character will always be "&" so slice it off
-            item.slice(0, -1);
+            item = item.slice(0, -1);
         }
         else {
             encodedValue = shouldEncode ? encodeURIComponent(value) : value;
@@ -448,7 +448,7 @@ const buildUrlParams = (params, shouldEncode = true) => {
         return `${accumulator}&${item}`;
     }, '');
     // Remove initial "&" from the reduce
-    return output.substr(1);
+    return output.substring(1);
 };
 /**
  * Build the RequestInit object based on the options passed into the initial request
